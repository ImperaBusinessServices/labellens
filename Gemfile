source "https://rubygems.org"

# Pins fastlane for reproducible CI runs. `bundle exec fastlane ...` in the
# GitHub Actions workflow resolves against this. Run `bundle install` once on
# a Mac to generate the committed Gemfile.lock.
gem "fastlane", "~> 2.220"
