// TEMPLATE — copy this file to Secrets.swift (same folder) and fill in the
// real values there. Secrets.swift is git-ignored so the real secret can
// NEVER be committed to this public repo; this .example file is the only
// version that gets committed.
//
// In CI, a workflow step writes Secrets.swift from GitHub Actions secrets
// before building, so the pipeline never needs the real file in git either.

import Foundation

/// Backend address of the verdict server (Keith's EC2 box).
let verdictServerURL = "https://REPLACE-WITH-YOUR-EC2-ADDRESS.example.com/verdict"

/// Must match LABELLENS_SHARED_SECRET in the server's .env. Sent as
/// "Authorization: Bearer <secret>" so strangers can't hit the endpoint.
let verdictSharedSecret = "REPLACE-WITH-YOUR-SHARED-SECRET"
